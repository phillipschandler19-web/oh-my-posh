from datasets import load_dataset

mindsFR = load_dataset("PolyAI/minds14", "fr-FR", split="train")from datasets import get_dataset_config_names

configs = get_dataset_config_names("PolyAI/minds14")
print(configs)from datasets import get_dataset_split_names

get_dataset_split_names("cornell-movie-review-data/rotten_tomatoes")void (*sa_handler)(int)  Pointer to a signal-catching function or one of the macros 
                         SIG_IGN or SIG_DFL. 
sigset_t sa_mask         Set of signals to be blocked during execution of the signal 
                         handling function. 
int      sa_flags        Special flags. 
void (*sa_sigaction)(int, siginfo_t *, void *)
                         Pointer to a signal-catching function. 
                         name: Merge contributions PR
on:
  pull_request_target:
    types:
      - opened
      - reopened

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code 👋
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
      - name: Check and merge ⛙
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3
        with:
          github-token: ${{ secrets.GH_PAT }}
          script: |
            const { repo: { owner, repo } } = context;
            const pr = context.payload.pull_request;

            if (pr.user.id !== 46447321) {
              console.log('Not an all-contributors pull request');
              return;
            }

            console.log(`Merging pull request: ${pr.html_url}`);
            await github.rest.pulls.merge({
              owner, repo,
              pull_number: pr.number,
              merge_method: "rebase",
            });
